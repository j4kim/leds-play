# either "pygame" or "neopixel"
driver = "pygame"

# for pygame driver only, controller is either "keyboard" or "gamepad"
controller = "keyboard"

# "prompt", "menu" or "light"
program = "menu"

# "pir" or "file"
motion_driver = "file"